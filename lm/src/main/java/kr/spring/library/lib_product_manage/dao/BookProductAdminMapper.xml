<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper
	namespace="kr.spring.library.lib_product_manage.dao.BookProductAdminMapper">
	
	<!-- 신규도서등록 -->
	<insert id="insertBookProduct" parameterType="BookProductVO">
	INSERT INTO lib_product_manage(
		lib_product_isbn,
		callNumber,
		lib_product_class_no,
		lib_product_bookname,
		lib_product_authors,
		lib_product_publisher,
		lib_product_bookImageURL,
		lib_product_publication_year,
		lib_product_description,
		lib_product_detail) 
	VALUES (
		#{lib_product_isbn},
		#{callNumber},
		#{lib_product_class_no},
		#{lib_product_bookName},
		#{lib_product_authors},
		#{lib_product_pulisher},
		#{lib_product_bookImageUrl},
		#{lib_product_publicatoin_year},
		#{lib_product_description},
		#{lib_product_product_status},
		#{lib_product_detail}
		)
	</insert>
	
	<!-- (검색가능하게) 관리자 도서목록 -->
	<!-- sql태그와 includ태그를 이용해서 SQL문을 재사용 -->
	<sql id="bookProductSearch">
		<where>
			<if test="keyword != null and keyword != ''">
				<if test="keyfield == 1">
					lib_product_bookName LIKE '%' || #{keyword} || '%'
				</if>
				<if test="keyfield == 2">
					lib_product_authors LIKE '%' || #{keyword} || '%'
				</if>
				<if test="keyfield == 3">
					lib_product_publisher LIKE '%' || #{keyword} || '%'
				</if>
			</if>
		</where>
	</sql>
	
	<!-- (개수)전체/검색 레코드수 -->
	<select id="selectRowCount" parameterType="map"
	                               resultType="integer">
		SELECT
		  COUNT(*)
		FROM lib_product_manage
		<include refid="bookProductSearch"></include>                               
	</select>
	
	<!-- 전체 도서 전체/검색 목록 -->
	<select id="selectList" parameterType="map"
	                           resultType="bookProductVO">
		SELECT
		  *
		FROM (SELECT
		        a.*,
		        rownum rnum
		      FROM (SELECT
		              *
		            FROM lib_product_manage
		            <include refid="bookProductSearch"></include>
		            ORDER BY callNumber DESC NULLS LAST)a)  
		<![CDATA[
		WHERE rnum >= #{start} AND rnum <= #{end}
		]]>                                     
	</select>
	<!-- 대출 도서 전체/검색 목록 -->
	<select id="selectLoanList" parameterType="map"
	                           resultType="bookProductVO">
		SELECT
		  *
		FROM (SELECT
		        a.*,
		        rownum rnum
		      FROM (SELECT
		              *
		            FROM lib_product_manage
		            WHERE lib_product_product_status=1
		            <include refid="bookProductSearch"></include>
		            ORDER BY callNumber DESC NULLS LAST)a)  
		<![CDATA[
		WHERE rnum >= #{start} AND rnum <= #{end}
		]]>                                     
	</select>
	
</mapper>